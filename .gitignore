# Real log files — may contain PII, never commit
samples/
*.log

# Python virtual environment — bulky, machine-specific
.venv/
venv/

# Python junk
__pycache__/
*.pyc

# Secrets — API keys for the AI layer later
.env
